#include <stdio.h>
/**
 * main - Printing nubers in single base
 * Return: Always 0 (Success)
*/
int main(void)
{
	int l;

	for (l = 0; l <= 9; l++)
	{
		printf("%d", l);
	}
	printf("\n");
	return (0);
}
