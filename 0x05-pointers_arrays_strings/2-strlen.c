#include "main.h"

/**
 * _strlen - provide the legnth of a string
 * @s: string
 * Return: Length
*/

int _strlen(char *s)
{
	int longt = 0;

	while (*s != '\0')
	{
		longt++;
		s++
	}
	return (longi);
}
