#include "main.h"

/**
 * _atoi - converts a function to a string
 * @s: the pointer to convert
 * Return: an integer
 */

int _atoi(char *s)
{
	int t, k, l, len, n, digit;
	t = 0;
	k = 0;
	l = 0;
	len = 0;
	n = 0;
	digit = 0;

	while (s[len] != '\0')
		len++;
	while (t < len && n == 0)
	{
		if (s[t] == 45)
		{

		}
	}
}
